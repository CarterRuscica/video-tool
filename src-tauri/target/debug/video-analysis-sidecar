#!/bin/bash
# Dev wrapper: runs the Python sidecar from source
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
BACKEND_DIR="$(cd "$SCRIPT_DIR/../../backend" && pwd)"
cd "$BACKEND_DIR"
exec python3 -m src.main "$@"
